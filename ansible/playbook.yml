---
- name: Configure App Server  
  hosts: app_servers
  become: true

  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: yes
    - name: Install Packages
      ansible.builtin.apt:
        name:
          - curl
          - docker.io

